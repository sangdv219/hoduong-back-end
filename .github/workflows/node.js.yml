# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Build Image Development CI/CD

on:
  push:
    branches: [ "main", "staging", "feat/**" ]
  workflow_dispatch:
    # logLevel: 'info'
  # pull_request:
  #   branches: [ "main", "staging" ] # production, staging

jobs:
  # lint:
  #   if: github.ref != 'refs/heads/main' && github.ref != 'refs/heads/staging'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Run ESLint
  #       run: npm run lint

  # test:
  #   if: github.ref != 'refs/heads/main' && github.ref != 'refs/heads/staging'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Run Tests
  #       run: npm run test --if-present

  # sonarQube:
  #   if: github.ref != 'refs/heads/main' && github.ref != 'refs/heads/staging'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci
        
  #     - name: SonarQube Scan 
  #       env:
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  #       run: npm run sonar 

  build:
    if: github.ref == 'refs/heads/staging'
    runs-on: [self-hosted, macOS]
    environment:
      name: staging  
      url: ${{vars.URL}}   
    env:
      USERNAME: sangdev2109
      IMAGE_NAME: hoduong-backend-app
      NODE_ENV: staging
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: hoduong-backend
      DB_USER: sangduong
      REDIS_HOST: redis
      PORT: 3000
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        clean: true
    - name: Log in to Hub Docker 
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_TOKEN }}
    - name: Create .env file
      run: |
        echo -e "ENV_CONTENT=staging" > .env.staging

    - name: Build docker image save version tag
      run: |
        echo -e "$ENV_CONTENT" > .env.staging
        docker build -f Dockerfile  --build-arg PF_ENV="${{secrets.ENV_CONTENT}}" -t ${{ env.IMAGE_NAME }} .
        docker tag ${{ env.IMAGE_NAME }}:latest ${{ env.USERNAME }}/${{ env.IMAGE_NAME }}:latest
    - name: Push docker image lastest and version tag
      run: |
        docker push ${{ env.USERNAME }}/${{ env.IMAGE_NAME }}:latest  
   
  deploy-staging:
    if: github.ref == 'refs/heads/staging'
    runs-on: [self-hosted, linux, arm64]
    needs: build
    permissions:
      contents: write
    environment:
      name: staging
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        clean: true

    - name: Copy file to Server
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.HOST_SERVER }}
        port: 22
        username: ${{ secrets.USER_SERVER }} 
        key: ${{ secrets.STAGING_SERVER_SSH_KEY }}
        source: "."
        target: "/home/sangduong/hoduong-backend-app"

    - name: Execute Deployment in Staging
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST_SERVER }}
        port: 22
        username: ${{ secrets.USER_SERVER }} 
        key: ${{ secrets.STAGING_SERVER_SSH_KEY }}
        script: |
          cd /home/sangduong/hoduong-backend-app
          # echo "---------- Start Deploying ----------"
           
          docker compose down
          docker compose pull
          docker rm -f hoduong-backend
          docker compose up -d 

          # echo "---------- Finished ----------"




  # production: